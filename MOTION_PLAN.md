# BangCut 모션그래픽 자동 삽입 — 기능 계획서

작성: 2026-08-12 · 상태: **초안(승인 대기)**
근거: 09 시퀀스 라이브 실측 + Video-Use/Hyperframes/Remotion 리포 병렬 조사(2026-08-12)

---

## 1. 목표

컷 편집 + 말자막까지 완료된 시퀀스에서 **중요 설명 구간을 자동 감지**하고, 그 지점에
**브랜드 모션그래픽(키워드 강조·리스트·비교·금액 카운터 등)을 생성해 타임라인에 삽입**한다.
보험 설명 채널 특성상 "핵심 개념을 열심히 설명하는 구간"의 이해 보조가 목적.

**제약(사용자 요구): 상단 상시 배너(09 시퀀스 V10 "상단 유의문구") 아래 영역에서만 그래픽이 디자인·배치될 것.**

## 2. 검토한 두 접근

### A. 노션 방식 — "대본만 있으면 됩니다"
대본 → 클립 계획서 → **고정 디자인 시스템**(팔레트·규칙) → Claude Code가 클립 코드 생성 → 렌더 → 프리미어 임포트 → 수정 시 버저닝.
→ 본질은 **방법론(워크플로우 규율)**: 계획서 파일, 디자인 토큰 고정, 버전 관리. 엔진 중립.

### B. 유튜브 방식 — Video-Use + Hyperframes/Remotion + ElevenLabs
전체 파이프라인(컷·자막·모션그래픽)을 Claude Code 스킬로 자동화. 조사 결과(핵심 사실):

| 도구 | 실체 | BangCut 관점 판정 |
|---|---|---|
| **Video-Use** (browser-use, MIT, ★20.5k) | ElevenLabs 전사→JSON EDL→ffmpeg 렌더. **산출물 = 구운 mp4뿐. 프리미어 XML/타임라인 내보내기·연동 전무** | **도입 안 함.** BangCut이 이미 하는 일(전사 기반 컷+자막)이며, 우리는 **편집 가능한 프리미어 타임라인**을 산출 — 전문 편집자에겐 결정적 우위. 차용만: 컷 경계 30ms 오디오 페이드, 프리뷰→셀프평가 루프 |
| **ElevenLabs STT** | 단어 타임스탬프 전사(유료 API) | **불필요.** `_words.json`(Vito/Whisper)이 동일 자산 |
| **Hyperframes** (HeyGen, Apache 2.0, ★~40k, 2026-04 공개) | **HTML→영상** 결정론 렌더(헤드리스 크롬+FFmpeg). **ProRes 4444 MOV 알파 렌더 공식 지원 — "투명 편집 중간물" 용도 명시.** GSAP/Lottie/Three.js. AI 에이전트 우선 설계(스킬 19종, 비대화형 CLI). 렌더 요금·상업 사용 제한 없음 | **채택(주 엔진).** 근거는 §3 |
| **Remotion** (★56k, 성숙) | **React→영상**. ProRes 4444 알파 공식 지원 | **백업 옵션.** 듀얼 라이선스(4인 이상 기업 유료, v5에서 라이선스 변경 예고) — BangCut을 공개 배포하는 입장에서 마찰. 템플릿 제작에 React 역량 필요 |

### 결론: **A의 방법론 + B의 엔진(Hyperframes)을 결합**
- 컷·자막은 **기존 BangCut 유지**(Video-Use 불채택 — 타임라인 상실이 치명적).
- 모션그래픽 클립 생성은 **Hyperframes**(HTML→ProRes 4444 알파 MOV).
- 워크플로우 규율(클립 계획서·고정 디자인 시스템·버저닝·세이프존)은 **노션 문서의 골격** 채택.

## 3. 엔진 선택 근거 — 왜 Hyperframes인가

1. **알파 렌더가 프리미어 임포트 정석 경로**: `npx hyperframes render --format mov` → ProRes 4444, 문서가 명시적으로 "NLE 편집 중간물" 용도로 규정.
2. **라이선스 무마찰**: Apache 2.0, 렌더 요금·인원 제한 없음. BangCut은 GitHub 공개 배포 도구 — Remotion(4인+ 기업 유료, v5 변경 예고)은 사용자에게 라이선스 숙제를 떠넘김.
3. **AI 에이전트 우선 설계**: BangCut 백엔드가 이미 Claude Code 헤드리스 — 스킬/비대화형 CLI 구조가 그대로 맞물림. 유튜버 실사용 평도 "품질 동급, 자유도는 Hyperframes 우위".
4. **HTML+GSAP**: Claude가 생성하기 쉽고, 사용자가 미세수정하기에도 React보다 진입장벽 낮음.
5. 리스크 헤지: 공개 4개월 신생(마찰 시 Remotion 전환 여지 — 아키텍처는 "알파 MOV 생성기" 인터페이스로 추상화해 엔진 교체 가능하게).

## 4. 아키텍처 (BangCut 철학 유지: 계산은 밖, 프리미어는 조립)

```
[1 구간 추출]  백엔드 클로드
   _words.json(현재 타임라인 재투영 시각) + 디자인 시스템 문서
   → motion_plan.json: [{t_start, t_end, type, title, lines[], version}]
     · type: keyword | list | compare | counter (보험 설명 4종 시작)
[2 검수 UI]  패널 (3번째 플로우: 컷 → 자막 → 모션)
   구간·문구 목록 → 사용자 켜고/끄기·문구 수정 (전량 자동 삽입 금지)
[3 클립 생성]  Hyperframes
   승인 항목별 HTML 컴포지션(고정 디자인 토큰) → ProRes 4444 알파 .mov
   파일명: <base>_mg_<n>_<type>_v<k>.mov (버저닝 — 노션 STEP 8)
[4 조립]  ExtendScript
   지정 트랙(배너 트랙 바로 아래 신설)에 시각 맞춰 삽입 (nodeId 식별 — 캡션 수정에서 검증된 방식)
```

- 시각 기준은 **현재 타임라인**(자막 재투영과 동일 원리) — 수동 다듬기 반영.
- 수정 루프: 검수 UI에서 문구 수정 → 해당 클립만 재렌더 v+1 → 교체 삽입.

## 5. 상단 배너 세이프존

- **1순위: 디자인 시스템에 하드 규칙** — 모든 컴포지션 HTML에 `top: <배너높이+여백>px` 이상 시작 강제(토큰 `--safe-top`).
- P0에서 09 시퀀스 배너 실제 픽셀 높이 실측 → `--safe-top` 수치 확정.
- 보조: 삽입 후 클립 position Y 오프셋 파라미터.

## 6. 로드맵

**개발 격리 원칙 (2026-08-12 확정)**: 모션그래픽은 **BangCut 리포 밖 별도 작업 폴더**에서 실험한다.
P0~P1 퀄리티 게이트 통과 시에만 BangCut에 정식 통합. 실패 시 BangCut은 무변경 — Node 의존성도 안 들어감.
롤백 지점: git 태그 **`core-stable-v0.2`** (컷편집+자막편집 완성 안정판, 커밋 270e855).

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **P0 프로토타입** *(별도 폴더)* | Hyperframes 설치 → 키워드 팝 1종 HTML → ProRes 4444 렌더 → ExtendScript로 트랙 삽입. 배너 높이 실측·세이프존 확정. 렌더 속도 실측(10초 클립 기준) | 지정 시각·트랙에 알파 그래픽이 뜬다 + **퀄리티 게이트: 사용자가 실물 보고 계속/중단 결정** |
| **P1 구간 추출** | 클로드가 전사에서 중요 구간+문구 추출 → motion_plan.json. 노션 원고 톤 반영(원문 텍스트 필요) | 09급 영상에서 그럴듯한 구간 10~20개 |
| **P2 검수 UI** | 패널 3번째 플로우. 목록 검수/문구 수정/일괄 렌더·삽입/버저닝 | 검수→삽입까지 원클릭 |
| **P3 라이브러리** | 템플릿 4종+(키워드/리스트/비교/카운터), 유형 자동 매칭, 스타일 스킬화(반복할수록 발전 — 유튜브 영상의 핵심 조언) | 실전 1편 투입 |

## 6.5 스타일 확장 — '07' 문법 자동화 (2026-08-12 추가, 사용자 확정)

09_Test 실증(보드·이모지 12개 삽입) 후, 사용자 자신의 편집 문법('07' 시퀀스 실측)으로 확장한다.

### 에셋 프로젝트 (사용자 제공 — 실존 확인됨)
`/Users/apple/Desktop/Bang's Work/VideoGraphy/Youtube/보험의정석 도종민/광고심의/광고심의필_음성안내.prproj`
- **"Zoom"** 조정 레이어 ← 줌인 자동화에 이걸 사용 (사용자 명명)
- 광고심의필_음성안내 (매 영상 맨 끝 삽입용)
- 상단 유의문구 (최상위 트랙 상시 배너)

### 자동화 대상 문법 (07 실측 + API 검증 완료)
| 문법 | 방법 | 검증 상태 |
|---|---|---|
| 줌인(105~120, 단발 115/120, 연속 벽돌 105→110) | Zoom AL 배치(overwriteClip) + Transform Uniform Scale 키프레임(addKey/setValueAtKey/ease) | API 존재 확인 ✓, 실증(P0-2) 대기 |
| 블러 전환(0→20/10f 후 자료 노출) | 동일 메커니즘 + Gaussian Blur(QE getVideoEffectByName) | 〃 |
| 인용 말풍선(😠+말풍선, 아래→위 팝) | Hyperframes 오버레이 | 메커니즘 실증 완료 ✓ |
| 강조 Mogrt 자막 구간 기본자막 off | 캡션 SRT 생성 시 해당 큐 제외 (SRT를 우리가 만들므로) | 설계 확정 |
| 찢은 종이 사이드 패널 | Hyperframes 보드 변형(반화면+알파) | 〃 |

### 세팅 리추얼 자동화 (제안 — 사용자 표준 워크플로우)
매 영상마다 수동으로 하던 것을 컷편집 완료 시 자동화 가능:
1. `importSequences`(API 확인 ✓)로 에셋 프로젝트 자동 임포트
2. **상단 유의문구** → 최상위 트랙에 영상 전체 길이로 삽입
3. **광고심의필_음성안내** → 컷편집된 영상 맨 끝에 삽입

### 줌 스펙 캘리브레이션
- .prfpset 적용 API는 없음 → 프리셋 파일은 자동화에 사용 불가
- 대신 **07 시퀀스의 실제 줌 키프레임(값·타이밍·ease)을 읽어 "사용자 줌 스펙"으로 추출** 후 코드로 재현
- 리스크: API ease는 열거형이라 프리셋 곡률과 미세 차이 가능 → P0-2에서 실물 비교·튜닝

### P0-2 (다음 실증, 승인 대기)
Zoom AL 임포트 → 09_Test 강조 모먼트 1곳에 배치 → 115줌 ease 키프레임 자동 → 실물 확인.

## 6.6 v2 재설계 (2026-08-13 샘플 승인 완료 — 파이프라인 실증됨)

사용자 실기 판정: ① 줌/블러 키프레임 여전히 미작동 ② 그래픽 디자인 불합격("중구난방") ③ 배경을 종이 질감 루프 영상으로.

### P0-3 결과 (2026-08-12 실측 확정)
- **줌(Transform)**: ✅ Plan A 성공 — Uniform 토글 없이 `Scale Height`+`Scale Width` 이중 키(5f·ease)가 렌더에 반영됨. 프레임 전수 검증 4/4 (단발 115·120, 벽돌 스택 105→110 포함). Uniform 토글은 같은 evalScript 안에서 프로퍼티명이 안 바뀌는 레이스가 있어 금지.
- **블러 키프레임**: ❌ 전 경로 실패 확정 — 신형 GB `Amount`·Legacy `Blurriness`·AL `Opacity` 모두 모델에는 키가 박히지만 **렌더러가 마지막 키를 상수로 사용**(절대·상대시간, app.refresh() 무효). GPU 계열 프로퍼티의 API 키프레임은 이 버전에서 신뢰 불가.
- **블러 대체 설계**: ✅ **정적 3-스택** — AL 3장(V5/V6/V7)을 3프레임 간격으로 쌓아 Blurriness 13→26(13+13)→40(+14). 키프레임 전무, 검증된 프리미티브(배치+정적값)만 사용. 프레임 검증 통과(선명→단계적 블러-인).
- 참고: 정적 값은 모든 이펙트에서 렌더 반영 확인(Amount=0 → 선명 즉시 반영).

### P0-3 키프레임 신뢰성 (최우선 게이트 — 이거 통과 전 대량 적용 금지)
- 이전 검증의 결함: 데모 1곳만 프레임 확인 — 전수 아님. 이번엔 **17줌+6블러 전수**: 키 상태 덤프 + **AL마다 램프 중간 프레임 익스포트**로 UI 진실 검증.
- 원인 후보: Uniform 토글 후 프로퍼티 핸들 stale / 보간 enum(5)의 실제 의미 / 키 시간 기준의 클립별 차이.
- **Plan B (API가 UI와 계속 어긋나면)**: 키프레임 API 포기 → **사전 키프레임 네스티드 시퀀스**(Zoom105/110/115/120: 5f ease 내장, Blur: 10f 0→20 내장)를 에셋 프로젝트에 두고 **배치만 자동화**(overwriteClip은 전 세션 검증 완료). 키프레임 리스크 원천 제거. 에셋 시퀀스 5개는 사용자가 1회 제작(또는 스크립트 생성 시도).

### 디자인 시스템 v2 — "말자막이 대본이다"
- **배경**: 사용자 루프 종이 질감 영상 `_템플릿/배경소스/Loop paper Texture/11.mp4` (4K·29.97fps·10.01s 루프 → 1080p 스케일, 10초 초과 보드는 루프 연장)
- **그래픽 영역**: 상단 배너 아래(y≈180) ~ 하단 말자막 위(y≈900) — 이 밴드 안에서만
- **레이아웃 템플릿 3종** (전부 그리드 정렬, 흩뿌리기 금지):
  - **T1 이모지+키워드 콤보**: 등간격 가로 그리드·동일 베이스라인, 멘트 발화 순서대로 팝
  - **T2 타이틀+요약**: 핵심 타이틀 + 말자막 압축 요약 2~3줄, 정렬 통일
  - **T3 타임라인 나열**: 좌→중→우 이모지+키워드 진행(연결선), 제도 변천·세대 설명용
- **정렬 규율**: 12컬럼 그리드 / 좌우 여백 120px 고정 / 타이포 스케일 고정(타이틀 88·본문 56·캡션 40) / 악센트 2색 / 등장 모션 통일(팝+오버슈트, 스태거 0.15s)
- **콘텐츠 파이프라인**: 재투영 말자막 큐 → 설명 단위 그룹핑 → 요약·구조화(제목/항목/강조어) → 템플릿 매핑

### v2 실증 결과 (2026-08-13)
- T1/T2/T3 샘플 3장 사용자 승인. **종이 배경 = 풀 16:9** (밴드 아님 — 상단 유의문구가 최상단 트랙이라 안 가려짐, 사용자 확정). 콘텐츠 존은 y180~900 유지
- 파이프라인 확정: Hyperframes 알파 MOV(GSAP, **발화 타이밍 싱크** — 각 요소가 해당 멘트 상대시각에 팝) → ffmpeg `-stream_loop -1` 11.mp4 배경 합성(ProRes 422HQ) → `(Footage)/Motion_v2/` → importFiles → **V8 큐 정합 배치** → 프레임 검증. 3건 실삽입 완료(1:11 T1 / 2:18 T2 / 5:21 T3)
- comp 소스: BangCut_Motion_Lab/keyword-pop/compositions/v2/ (t*=스틸, a*=애니), 렌더: `npx hyperframes render . -c <comp> --format mov --fps 30000/1001 -q high`

### 작법 3원칙 (2026-08-14 사용자 피드백 — 필수 규율)
1. **중복 금지**: 캡션 문장을 MG에 복제하지 않기 — MG는 압축 키워드만 ("병원비 영수증엔 두 종류가 있다" ✗ → "병원비 영수증" ✓)
2. **스포일러 금지**: 화자가 아직 말 안 한 정보 선행 노출 금지 ("비급여, 둘로 쪼개진다"를 쪼개진다 발화 전에 띄움 ✗)
3. **발화 후행 인크리멘털 빌드업**: 요소는 해당 멘트 직후 등장, 서브 요소도 큐 단위로 분해 (T3 예: "재가입 주기가 없습니다" 발화 때 "주기 없음" → "조건은 평생 가는 겁니다" 발화 때 "조건 평생" — 카드 한 번에 ✗)
- 메인 타이틀 위계: 폰트 크게 + **《 》/<> 브래킷** 김도영 스타일 강조
- 사용자 T2 교본: "비급여" 타이틀 → (쪼개는 겁니다 발화) 중증/비중증 분기 → (각 설명 발화) 화살표+결과 추가 — **"말을 따라 자라나는 다이어그램"**

### 강조자막 확장 — 즐겨찾기 Mogrt 29종 스마트 매칭 (2026-08-14)
- 소스 폴더 확정: `_템플릿/Mogrt/` 29개 (사용자 제공)
- 태깅 초안(썸네일 실측): 001~004 L/R 8종=우아한 명조 인용/서술(금액 이탤릭 변형 포함), 005=단호 경고(민트 글로우), 006=포인트 강조, 007=질문/공감 명조, 008=도발 질문, 009=긍정 회고, 010·011=아래-in 구어 멘트, 012=부정/반전 "아닙니다", 013=부담/우울 보라, 014=🔥경고 배너, 015=축하 핑크, 016=금박 혜택, 019=심각 아웃라인, 022=혼합 강조, 023 ×3=순차 키워드 세트
- 파이프라인: 큐 내용 무드 분석 → 태그 매칭 → 바이트 패치 클론 → importMGT. **29종 전수 패치 호환성 테스트 1회 필요**(다중 텍스트 레이어 템플릿 존재)

### C1 강조 큐 처리 확정 (2026-08-14)
- 자동: 강조 큐 제외한 SRT 재생성 → 새 캡션 트랙 삽입
- 수동은 **옛 트랙 눈(👁) 끄기 1클릭이면 충분** (삭제 불필요 — 사용자 확인). 눈 토글 API는 없음(캡션 트랙 API 전무)

### 줌 프리셋 자동화 (D&D 폐기 목표 — 사용자 요구)
- 후보 A **prproj XML 수술**: .prfpset(gzip XML)에서 베지어 곡선 원본 추출 → 저장된 .prproj의 해당 AL 클립에 Transform 컴포넌트 주입(mogrt 수술과 동일 기법) → **프리미어 재시작 필요**, 백업 필수. 결정론적 — 1순위 실험
- 후보 B **비전 GUI 자동화**: 어도비 패널은 AX 미노출이지만, **화면 기록(Screen Recording) 권한**까지 있으면 스크린샷→좌표 인식→마우스 이벤트로 D&D 재현 가능. 화면 배치 의존 리스크
- 현행(그룹 선택+수동 D&D 30초/영상)은 A/B 성공 전까지의 폴백

### 다음 테스트 스펙 (사용자 확정)
**V3~V9 전량 삭제 후 실전 풀 자동 재생성**: 줌 AL(§6.8 컷스냅+파일수술, 분당 4~6개·120 비중 상향) + 강조자막(29종 매칭, 분당 8~11개) + 모션그래픽(3모드·작법 3원칙) + **말풍선 인용 팝업** + **SFX 페어링(§6.10, A3 배치)** + 인트로 연타 + 캡션 트랙 교체까지 한 번에. 완급 조절(도배 금지) 유지

### 풀 재생성 테스트 결과 (2026-08-14 — 완료 ✅)
- **전량 성공**: 줌 AL 55(수술 55/55, 스케일 분포 120×17/110×20/105×13/115×5, 5.2/분) + 강조 mogrt 69(29종 매칭, 비보드 9.6/분) + MG 보드 7(T1~T3 재사용 + T4숫자/T6면책/T7재매입/T8할인 신규, 보드 32%) + 말풍선 7(GSAP 7f 아래→위, V9=보드 위) + 사이드 오버레이 3(모드2 신규, V6) + 구독 MOV(V4→오디오 A4 자동) + SFX 107(A3/A4, 무드 로테이션) + 캡션 교체(88큐 제외 SRT 114큐)
- **importMGT 크래시 버그 (프리미어 26.3)**: 세션당 ~15-30회 호출 시 세그폴트(시스템 리소스 무관, 내부 누적 버그). **우회 = 세션당 12개 상한 + 선제 clean 재시작 사이클**(1개/호출 + 매회 save + 3초 간격). 크래시 후 자동복구 세션은 **최종 저장분보다 옛 상태**일 수 있음 — 복구 직후 save 금지, 먼저 probe로 비교 (이번에 54→32 롤백 실수 1회)
- **🎉 `sequence.createCaptionTrack(item, 0, 2)` API 존재·성공** — SRT 임포트 후 캡션 트랙 자동 생성됨. 수동은 옛 C1 눈 끄기 1클릭뿐
- **🎉 캡션 스타일 승계 = 파일 수술로 해결(2026-08-14, cap_style_surgery.py)**: 캡션 큐 = CaptionDataClipTrackItem → Block → FormattedTextData(FlatBuffers 블롭 = 스타일+텍스트, BinaryHash 콘텐츠 주소 dedup — 같은 해시는 빈 태그로 참조). C1 블롭엔 폰트(EliceDXNeolliOTF-Lt)·배경까지 전부 내장 → **같은 텍스트의 C1 블롭 해시로 C2 Block 참조를 바꿔치기**하면 스타일 이식 완료(바이너리 패치 불필요). 114/114 성공, 프레임 실증 — C1과 픽셀 동일 렌더. 텍스트가 C1에 없는 큐는 이식 불가(전량 매칭이라 실전 무관)
- 구독 MOV 배치 규칙: videoTracks[N].overwriteClip → 오디오는 같은 인덱스 audioTracks[N]에 랜딩(V4→A4 실측). A1 대사 보호 위해 빈 A트랙 인덱스의 V트랙 선택
- QE exportFramePNG는 확장자 중복(.png.png) 생성됨
- 검증: Transform 스팟체크 5/5 정확, 프레임 7장 — 보드 발화 타이밍 정합(170s에 카드 2/3만 표시 = 정확), 말풍선/SO/줌/강조 전부 정상

### 승인 게이트 (품질 프로세스)
1. **디자인 샘플 3장**(T1/T2/T3 각 1장, 실제 멘트, 정지 PNG) 제작 → **사용자 승인/수정**
2. 승인 디자인으로 **전체 모먼트 플랜 표**(시각·템플릿·문구·이모지) 보고 → **사용자 승인**
3. 승인분만 렌더 → 기존 V8 그래픽 전량 교체
4. P0-3은 병렬 진행, 전수 통과 후에만 줌/블러 확정

## 6.7 전체 워크플로우 자동화 지도 (2026-08-13 사용자 최종 명세 반영)

### 사용자 확정 명세 (2026-08-13 원문 기준)
**전제**: BangCut으로 ① 자동 컷편집 완료 ② 자막 편집 완료 — 그 다음부터 이 워크플로우 시작.

1. **에셋 프로젝트 임포트**: `보험의정석 도종민/광고심의/광고심의필_음성안내.prproj` — Import Project 다이얼로그에서 **Import Entire Project** + **Create folder for imported items** 체크(중복 미디어 허용 OFF). 수동 임포트 시 다이얼로그가 뜨는 구조 — 스크립트 임포트(importFiles) 시 다이얼로그 발생 여부는 P0 테스트에서 확인, 발생하면 옵션 사전 지정 또는 GUI 폴백.
2. **광고심의필_음성안내 → 영상 맨 끝** 삽입, **해당 클립의 오디오는 A1 트랙**으로.
3. **상단 유의문구 → 최상위 V트랙**, 영상 시작부터 **광고심의필_음성안내의 끝점까지** (원본 영상 끝이 아님 — 부록 구간 포함 전체 길이로 트림).
4. **강조자막** (C1 큐 단위): 조정 레이어로 줌인 + 자막 내용에 어울리는 **Mogrt 삽입 + 해당 C1 텍스트를 Mogrt에 주입** + **해당 C1 큐는 Disable**(안 보이게). Mogrt·조정 레이어 모두 **C1 큐의 시작점·끝점에 정확히 일치**.
5. **모션그래픽** (C1 큐 단위): 해당 **C1 큐는 끄지 않음**(강조자막과 다름). 영역 = **상단 유의문구 아래 ~ 하단 말자막 위**. 애플 이모지 + 핵심 키워드. 긴 설명·타임라인식 순차 설명 구간은 **왼쪽→중앙→오른쪽** 순서로 이모지&키워드 정렬(이해 시너지). 배경 = **움직이는 종이 질감 영상**, 클립이 짧으면 **루프로 연장**. **모션그래픽 클립 끝점 + 종이 질감 배경 끝점 = C1 큐 끝점에 정확히 일치**.

### 단계별 자동화 판정
| # | 작업 | 자동화 | 방법 · 근거 |
|---|---|---|---|
| 0 | BangCut 컷편집 + 자막편집 | ✅ 기존 | 선행 조건 (BangCut 본체) |
| 1 | 에셋 프로젝트 임포트 (Entire + 폴더 생성) | ✅ 완전(테스트 1회 필요) | importFiles/importSequences (검증) — 다이얼로그 발생 여부 P0 확인 |
| 2 | 광고심의필_음성안내 → 영상 맨 끝 (오디오 A1) | ✅ 완전(테스트 1회 필요) | V1 끝시각 계산 + audioTracks[0].overwriteClip |
| 3 | 상단 유의문구 → 최상위 V트랙, 시작~광고심의필 끝점 | ✅ 완전 | 배치+end 트림(검증). 끝점 = 광고심의필 클립 end. 트랙 부족 시 QE addTracks |
| 4 | Skin AL → V2 전체 길이 | ✅ 완전 | 동일 프리미티브(검증) |
| 5 | 모션그래픽 클립(종이배경 임베드) → C1 큐 시작·끝 정합 | ✅ 완전 | 최종 C1 큐 기준 렌더 길이·배치 일치, 배경 루프 연장 포함 (C1은 켜둠) |
| 6 | Mogrt 강조자막 → V5~V6, C1 큐 시작·끝 정합 + 텍스트 주입 | ✅ 완전(테스트 1회 필요) | `sequence.importMGT(path,time,trackIdx)` 공식 API + end 트림 + getMGTComponent로 텍스트 주입 |
| 7 | 강조 구간 C1 큐 Disable | ⚠️ 대체 방식 | 캡션 트랙은 API 접근 불가(실측) → 아래 C1 아키텍처의 옵션 (a)/(b) |
| 8 | Zoom AL → 강조 구간 V3~V4 (큐 시작·끝 정합) | ✅ 배치 완전 | 컷·큐 스냅 배치(검증) |
| 9 | 줌 프리셋 105~120 적용 | 🔶 반자동 | §6.8 확정 워크플로우 — 그룹 선택(자동) + 사용자 D&D(~10초/그룹) + 전수 검증(자동) |

### C1 최종본 기준 아키텍처 (2026-08-12 사용자 요구 반영)
사용자 요구: 판단·정렬 기준은 우리가 생성한 SRT가 아니라 **사용자가 최종 다듬은 C1 캡션**이어야 하며, 모션그래픽/강조 판단은 자막 완성 이후에 내려진다.

- **C1 직접 읽기 API 없음 — 리플렉션으로 최종 확정** (Sequence 메서드 전수 덤프: createCaptionTrack 생성만 존재)
- **우회로: `exportAsMediaDirect` + 캡션 사이드카 .epr 프리셋** → 최종 C1을 SRT 사이드카로 추출해 진실원본으로 사용 (오디오-온리 프리셋이면 고속). .epr 1회 제작 필요. → P0 테스트 항목
- 파이프라인 재정의:
  자막 완성(사용자 다듬기 포함) → [자동] 사이드카 익스포트로 최종 C1 확보 → [자동] 최종 큐 기준으로 모션그래픽/강조/줌 판단 → [자동] 큐 시작·끝 정합 배치
- **강조 큐 끄기**(disable API 없음) 실무 옵션:
  (a) 강조 큐 타임코드 리스트 제공 → 사용자가 클립 선택+D (영상당 1~2분)
  (b) **추천**: 최종 C1에서 강조 큐 제외한 새 캡션 트랙 재생성 → 사용자는 옛 트랙 1개만 삭제 (클릭 1회)

### 확정 제약 2개 (정직)
1. **프리셋 키프레임 적용은 반자동** — 사용자 paste 30초/영상. (붙여넣은 키의 '값'만 API 수정이 재생에 반영되는지 실험 1회 예정 — 성공 시 paste 1회로 단축)
2. **C1 캡션은 API로 읽기/끄기 불가** — 우리 큐 데이터가 진실원본. 사용자가 C1을 수동 수정한 경우 그 변경은 자동화가 인지 못 함(재투영으로 재생성은 가능)

### 블러 폐기 → 종이질감 영상 삽화 (2026-08-12 확정)
블러 전환 제거. 그래픽 모먼트 = **11.mp4(움직이는 종이 질감)를 컴포지션 배경으로 임베드**한 MOV 1클립 — 타임라인 오염 최소.

## 6.8 줌 자동화 최종 확정 (2026-08-12 실전 완료 — 18/18)

### 확정 워크플로우 (검증 완료)
1. [자동] 맨몸 Zoom AL 배치 — 발화 앵커 + 컷 경계 스냅(시작·끝), 스케일 그룹별 (V3 단발/브릭하단, V4 브릭상단)
2. [자동] 그룹 선택 (setSelected)
3. [수동 ~10초/그룹] 사용자: Effects > Presets의 줌 프리셋을 **선택된(하이라이트) 클립 위에 드래그 앤 드롭** → 선택 전체에 적용
4. [자동] 전수 검증 (Transform 존재 + 최종 키 값 = 목표 스케일)

### 핵심 지식 (하드런)
- **키프레임 좌표계 = 클립 소스 시간(인포인트 기준)** — Zoom AL 아이템의 기본 인포인트 ≈ 3599.996s. 시퀀스 0기준/타임코드 기준 모두 오답이었음
- API 키프레임은 좌표만 맞으면 박히지만, **사용자 프리셋의 커스텀 velocity 곡선·셔터 모션블러는 API로 재현 불가**(베지어 핸들 API 없음) → 프리셋 원본 적용이 정답
- 프리셋 **더블클릭 = 속성 창**(적용 아님). **선택 클립 위 드래그 앤 드롭 = 선택 전체 적용**
- 비선택 클립에 드롭하면 그 클립에만 적용됨 (사용자 실수 포인트 — 스킬에서 검증 단계 필수)
- 이펙트 컴포넌트 제거 API 없음 → 오적용 시 클립 삭제 후 재배치가 정석
- 사용자 프리셋 실측: Uniform ON, 단일 Scale 키 2개(인포인트+0 → +5f), Anchor to In Point 타입

## 6.9 E2E 실전 검증 완료 (2026-08-13 — 09_Test 풀 파이프라인)

### 실행 결과 (전부 실측 성공)
1. 기초 세팅: 광고심의필→V1 끝+A1 / 상단 유의문구→V10 0~광고심의필 끝점 / Skin AL→V2 0~영상 끝 (AL end 연장 5s→635s 가능)
2. **최종 C1 읽기 확정**: `exportAsMediaDirect` + .epr — 사용자 `최종광고물 렌더링.epr` 복제 후 `DoVideo=false` + **`ADBECaptionExportOption=1`(=사이드카)**로 수정 → `<출력>.srt` 사이드카 생성. **타임스탬프 = 시퀀스 절대시간** → 큐 정합에 그대로 사용. 전체(10.5분) 오디오-온리 익스포트 ~4분. 구간 익스포트(in/out, workAreaType=1)도 동작
3. **mogrt 텍스트 주입 완전 해결 — 바이트 패치 클론** (`mogrt_build.py`): mogrt(zip)⊃prgraphic(zip)⊃prproj(gzip XML)의 `<Name>Source Text</Name>` 블록 StartKeyframeValue blob = `…+길이(LE32)+UTF-8+패딩` → 텍스트 교체 후 재조립 → importMGT → **스타일 완벽 유지 렌더**. 수동 개입 0
4. 강조자막 11개: 클론 mogrt(V5) + Zoom AL(V3) 큐 시작·끝 정합 배치 → 프리셋 D&D 3라운드(115×6/120×4/110×1) → 전수 검증 11/11

### 파괴 사고 & 확정 가드 (최중요)
- **에셋 prproj 재임포트 = 파괴적**: 이미 임포트된 프로젝트를 importFiles로 다시 임포트하면 중복 억제가 기존 마스터(Zoom·MGT Media)를 교체하며 **참조하던 트랙 클립 전멸** (probe→import→probe 10초 창에서 실측 확정). → **스킬 필수 가드: 임포트 전 존재 체크, 있으면 스킵**
- importFiles(prproj, suppressUI=true)는 다이얼로그 없이 전체 임포트됨 (최초 1회는 안전)
- 복구 지점: 메인 .prproj는 저장 전까지 무사 + Auto-Save 폴더 타임스탬프별 스냅샷 (오토세이브 로테이션 전에 복사 백업)

### API 함정 추가분
- **Source Text setValue(문자열) = 렌더 파괴** (모델은 바뀌지만 TextDocument 구조 깨져 텍스트 소멸). getValue는 불투명 핸들(코드 992 1글자). getMGTComponent()는 프리미어 네이티브 mogrt에서 null (isMGT=true여도) → 바이트 패치 클론이 유일한 자동 텍스트 경로
- **responsive pinning mogrt는 Motion Scale 상쇄** (Left/Right 핀 + Parent Width) — 클립 스케일로 텍스트 크기 조절 불가 → **강조 텍스트는 짧은 펀치라인(≤11자)으로 압축 생성**이 정답 (실무 문법과도 일치)
- 강조 구간 C1 큐 Disable은 여전히 수동 (타임코드 리스트 제공 방식)
- 한글 리터럴 evalScript 전달 시 NFC/NFD 불일치 함정 — 아이템 매칭은 인덱스/ASCII로, 파일 경로는 NFD 정규화

## 6.10 스타일 가이드 — 실측 기반 (2026-08-14, '07'+'01(Renewal)' 전수 덤프 + 김도영 aWptenwljS0 분석)

### 밀도 기준 (01(Renewal) 실측 — 최신 문법)
- **줌: ~6개/분** (460초에 47개). 스케일 분포: 120×15, 110×14, 105×7, 115×4, 줌아웃 70×1. 브릭 스택 10회
- **강조자막: ~11개/분** (V5 61개 + V6~V8 스택 ~36개) — 거의 상시. 다수가 줌과 시간 정합(페어링)
- 인트로 0~15초 = 하이텐션 연타 (줌 5 + 그래픽 6)
- '07'(구작)은 줌 2.6/분·110 위주 → 01에서 밀도·120 비중 급증 = 문법 진화 방향
- 테스트(1개/분)는 실전 대비 1/6~1/10 밀도 — "적다" 피드백의 정량 근거

### 사용자 고유 문법 요소 (자동화 매핑)
- 줌+강조 동시 페어링 + 단독 줌 다수 (대사 리듬)
- **SFX 페어링**: 그래픽/강조 등장마다 효과음 — A3 73개 + A4 11개/영상. **자동화 확정 → 아래 "SFX 자동화" 섹션**
- 챕터 리추얼: 챕터 트랜지션.mov + [Make It Cool] Thurston 3 타이틀 + 미러 그래픽 3트랙 + SFX
- 구독 유도: 구독 알림설정.mov 중간 1~2회
- 말풍선 01~08 네스티드 시퀀스(인용/재연), 댓글자랑
- '07'의 Blur+자료.png+찢은종이(diy-ripped-paper, 34초) 다층 구간 = **긴 설명(20~40초) 구간 문법 → 모션그래픽 보드로 대체 대상**(사용자 확정)
- 강조자막 실체: 대부분 프리미어 네이티브 Graphic(Vector Motion+Text, 템플릿 수정본) + mogrt 계열(무료템플릿81/86/111, by.Lily #28, 분노 자막류, 비트비트체)

### 말풍선(인용 팝업) 자동화 (2026-08-14 사용자 추가 요청)
- 실측('01(Renewal)' 2:11 V5 말풍선 01): 네스티드 시퀀스(애플 이모지😱 + 말풍선 셰이프 + 내부 텍스트) + **Transform(04_아래in): Position (960,1204)→(960,540) 아래→위, ~7프레임, ease 감속 곡선(피크 ~5000px/s)**
- 용도: 화자가 타인의 주장/반응을 인용·예시할 때 ("정말로 그런 게 있었어요!?")
- **자동화 방식 (신규 기계장치 불필요)**: Hyperframes 말풍선 템플릿(이모지+버블+텍스트, 알파) 렌더에 **7f 아래→위 ease 팝업을 GSAP으로 내장** — 곡선은 실측 velocity 프로파일 모사(expo.out 계열). 큐에서 인용/놀람 발화 감지 → 텍스트·이모지 생성 → V 트랙 큐 정합 배치. C1은 유지
- 폴백: 정확 곡선이 필요하면 01(Renewal).prproj에서 04_아래in Transform 도너 추출(줌 수술과 동일 기법)해 주입 가능
- 무드 이모지 매핑: 놀람😱 분노😡 의문🤔 좋음😊 눈물😢 등 발화 톤 기반

### SFX 자동화 (2026-08-14 확정 — '01(Renewal)' A3+A4 84개 전수 상관 분석)
- **실측 규칙**: 84개 중 81개가 V트랙 시각 이벤트의 **시작점과 ±0.25초 이내 동시 시작** — "SFX = 시각 요소 등장음, 클립 시작에 스냅"이 유일 규칙. 나머지 3개는 뿅×3 연타(0.3초 간격)로 MG 내부 서브요소 순차 등장에 대응(작법 3원칙 ③의 오디오 버전)
- **페어링 실측 분포**:
  - 강조자막(Graphic) → 자막_ 계열: 따탁×10, 따랑×9, 뚜둥×8, 와우밈×5, 휙×4, 샬랄라×3, 뿅×3, 황당×2, 도오옹×2, 맑은벨×2, 눈물×2, 쿵/쿵!!/탁탁×1씩 — 무드별 로테이션(같은 소리 연속 도배 없음)
  - 말풍선 01~04 → **드릉 4/4 (100% 고정)**
  - 소품/이모지/카드 등장(png, 댓글자랑, 브릭) → **뿅 고정**
  - 챕터 트랜지션.mov·구독 알림설정.mov → 자체 내장 오디오(별도 SFX 불필요)
  - 후광 연출 → 천사의 강림, 부정/오답 멘트 → 땡·쿵, 반전 → 놀람 반전
- **템플릿 라이브러리**: `~/Desktop/Bang's Work/VideoGraphy/_템플릿/사운드` 55종, 접두사 체계(강조_/자막_/트랜지션_/효과음_)가 그대로 무드 태그 — 구명(뿅→자막_POP, 휙→트랜지션_휙, 쿵→강조_쿵 3피치 등) 매핑만 하면 됨
- **구현 (신규 기계장치 불필요)**: 모먼트 플랜이 이미 모든 시각 이벤트의 t0를 앎 → 무드 매칭한 mp3/wav를 importFiles(신규 미디어=안전) 후 `audioTrack.overwriteClip(item, t0)`로 A3 배치(겹침 시 A4 폴백). 파일 경로 NFD 정규화 필수
- 규칙: 강조자막·말풍선·MG 서브요소·소품 등장마다 1발, 무드 로테이션으로 중복 연타 방지, 챕터/구독 mov는 스킵

### 레퍼런스 파일 해부 실측 (2026-08-14, '07'+'01R' prproj 오프라인 분석 — ref_style_db.json)
- **강조자막 문구 규칙 (71개 Graphic↔캡션 페어 실측)**: ① **원문 최대 보존 + 어미만 구어체 다듬기** ("안 되겠습니다"→"안됩니다!", "때문입니다"→"때문!", "하시겠죠"→"하시겠죠~~") ② **긴 캡션은 발화 따라 2클립 시분할** ("백내장 수술했는데"→"100만 원 주는 보험!!") ③ **또는 한 클립 2레이어 상하 분할** ("여러분들이라면 | 이런 보험 해지하시겠습니까?") ④ **나열 발화는 단어별 개별 클립 등장·유지** (질병코드/수술분류표/급여・비급여 각 발화 순간) ⑤ 스타일용 동일 텍스트 2레이어 에코 존재. **≤11자 축약 정책 폐기** — 09_Test 피드백과 실측 모두 원문 우선
- **'07' Blur AL 실측**: AE.Impact_Blur_FX(서드파티) Amount 0→20 (0.33s=10f 리니어 인/아웃, interp=0) + AE.ADBE Tint "Amount to Tint" 65 (사실상 고정). 새 구현은 사용자 지정: **가우시안 블러 + 5프레임 리니어 인/아웃 + Tint 고정** — 키프레임은 파일 수술로 주입(API 블러 키 렌더 무시 확정이므로)
- **말풍선 내부 실측 ('01R' 네스티드 4종)**: 말풍선.png Position **정규화 (0.49~0.56, 0.85) = 하단 캡션 존(y≈920px)**, Scale 100. 내부 = 말풍선.png + 텍스트 레이어(구어체 위트: "정말로 그런게 있었어요!?") + **😱 애플 이모지도 텍스트 레이어**. 말풍선 = 캡션 대체(해당 큐 캡션 제외 필요)
- **댓글자랑 실측**: "나 1-3종 보험 있는거 실화임!?!" + 핸들 "@na-jom-jalnaga" — 유저 댓글 재연 그래픽 문법
- **가이드 라인 확정 (2026-08-14 사용자 실측 공유)**: ① 캡션 자막·강조자막 = **세로 중심 y≈975px** (가이드 선 975에 텍스트 중앙 정렬) ② 말풍선 = **세로 중심 y≈920px** (파일 실측 0.85×1080=918과 일치 — 교차 검증됨). 자동 생성물 전부 이 기준선 준수
- **B-roll 플레이스홀더 (2026-08-14 사용자 추가)**: 사용자는 엔바토(Envato)에서 멘트와 어울리는 스톡 영상을 B-roll로 다수 사용 — 자동화는 B-roll이 어울리는 구간(상황 묘사·사례 서술·비유 발화)을 감지해 **풀 16:9 검정 배경 + 중앙 "B-roll" 텍스트 플레이스홀더 클립**을 삽입하고, 구간 목록+추천 사유(검색 키워드 제안 포함)를 보고 → 사용자가 엔바토에서 골라 교체. 모드3(B-roll+플로우) 공백을 이 방식으로 해소
- 014 등 **byte-patch 후 렌더 깨지는 mogrt 존재** — 전수 실렌더 검증(식별 텍스트+프레임 추출)으로 템플릿별 레이어 맵·렌더 성공 DB 필수 (021처럼 Source Text 아닌 이름의 추가 텍스트 레이어도 이때 발견)

### 김도영 3모드 (모션그래픽 레이아웃)
1. **풀보드**: 종이질감 풀스크린 + 《브래킷 골드 타이틀》 + 손글씨풍 서브 — 정의/챕터 도입
2. **사이드 오버레이**: 화자 유지, 옆 공간에 대형 이모지 + 빨강 브러시 키워드 + 보조어 — 짧은 강조 (배경 없음·알파)
3. **B-roll+플로우**: 스톡영상 위 이모지 카드 좌→우 화살표 다이어그램
- 그래픽 없는 순수 발화 구간도 유지 (완급 조절 — 전 구간 도배 금지)
→ 현재 파이프라인은 모드1만 구현 — **모드2(알파 사이드 오버레이) 추가 필요**

## 7. 리스크 (솔직한 것들)

1. **렌더 속도** — 헤드리스 크롬 프레임 단위 캡처라 실시간보다 느림. 10초 내외 클립 단위로 쪼개는 설계로 완화. P0에서 실측 후 판단.
2. **Node 22+ 의존성 추가** — 현 스택(Python+ffmpeg)에 이중 런타임. 지인 배포 시 자동 설치 스크립트 확장 필요.
3. **구운 영상 = 프리미어 내 텍스트 수정 불가** — 오탈자는 재렌더 필요. 검수 UI에서 삽입 전 문구 확정 + 클립 단위 재렌더 루프로 완화. (반대급부: 에센셜 그래픽/mogrt보다 표현 자유도·자동화 적합성 압도적)
4. **신생 프로젝트**(공개 4개월) — 엔진 인터페이스 추상화로 Remotion 전환 퇴로 확보.
5. **구간 추출 품질** — "중요함" 판정은 주관적 → 검수 UI 필수(전량 자동 삽입 안 함).
6. 노션 문서 세부(디자인 토큰·프롬프트 원문)는 스크린샷 해상도로 판독 불가 — 원문 텍스트 필요.

## 8. 다음 액션 (사용자)

- [ ] 계획 승인 / 수정 지시
- [ ] 노션 문서 중 **디자인 규칙·프롬프트 부분 원문 텍스트** 전달 (스크린샷은 구조만 판독됨)
- [ ] P0 착수 승인 (Hyperframes 설치 + 키워드 팝 1종 실증)
